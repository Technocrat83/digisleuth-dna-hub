Federation/Synchronization_Model.md:
  purpose: >
    Defines synchronization mechanics between nodes,
    payloads, memory surfaces, and execution layers.

  synchronization_types:
    - read_only
    - write_allowed
    - bidirectional
    - quarantined
    - governance_locked

  primary_focus:
    - synchronization_coherence
    - semantic_alignment
    - synchronization_pressure
